/*
 * Arduino_dsPIC33CK.h
 *
 * This library exists to carry the platform's example sketches, not to add API.
 *
 * Arduino assembles File > Examples from installed *libraries* only. A
 * platform-level examples/ directory -- which is where these sketches lived
 * until 1.0.1 -- is never scanned, so every board example was invisible in the
 * IDE even though it shipped in the archive. Moving the tree under a bundled
 * library is what makes it appear, and is the same arrangement arduino-esp32
 * uses for its libraries/ESP32 examples.
 *
 * A library needs a header to be a valid 1.5-format library, so this is it.
 * Including it is equivalent to including Arduino.h; the examples themselves
 * include Arduino.h directly and do not depend on this file.
 */

#ifndef ARDUINO_DSPIC33CK_H
#define ARDUINO_DSPIC33CK_H

#include <Arduino.h>

#endif /* ARDUINO_DSPIC33CK_H */
